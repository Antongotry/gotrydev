# 📝 Как коммитить обновления плагина

## Когда вы обновляете кастомный плагин `universal-license-manager`:

### Шаг 1: Проверьте изменения
```bash
cd /Users/mac/Downloads/GOTRYDEV
git status
```

Покажет, какие файлы изменились.

### Шаг 2: Добавьте измененные файлы плагина
```bash
# Добавить все изменения в плагине
git add wp-content/plugins/universal-license-manager/

# Или добавить конкретный файл
git add wp-content/plugins/universal-license-manager/universal-license-manager.php
```

### Шаг 3: Закоммитьте изменения
```bash
git commit -m "Update plugin: описание того что изменили"
```

**Примеры сообщений коммитов:**
- `"Update plugin: добавил новую функцию генерации лицензий"`
- `"Update plugin: исправил баг с сохранением данных"`
- `"Update plugin: обновил стили админ-панели"`

### Шаг 4: Запушьте на GitHub
```bash
git push origin main
```

## 🔄 Быстрая команда для обновления плагина:

```bash
# Всё в одной строке:
git add wp-content/plugins/universal-license-manager/ && \
git commit -m "Update plugin: описание изменений" && \
git push origin main
```

## 📋 Полный пример:

Допустим, вы обновили файл `universal-license-manager.php`:

```bash
# 1. Проверка изменений
git status

# 2. Добавление файла
git add wp-content/plugins/universal-license-manager/universal-license-manager.php

# 3. Коммит с описанием
git commit -m "Update plugin: добавил валидацию email адресов"

# 4. Отправка на GitHub
git push origin main
```

## ⚠️ Важно:

- Всегда проверяйте `git status` перед коммитом
- Пишите понятные сообщения коммитов
- Коммитьте только файлы плагина `universal-license-manager`
- Не коммитьте чувствительные данные (пароли, ключи API)

## 🎯 Обновление темы:

Если обновляете тему (не плагин), используйте те же команды, но для файлов темы:

```bash
git add style.css functions.php assets/
git commit -m "Update theme: описание изменений"
git push origin main
```

## 📦 Обновление и темы, и плагина одновременно:

```bash
git add wp-content/plugins/universal-license-manager/ style.css functions.php
git commit -m "Update theme and plugin: описание изменений"
git push origin main
```
