# 🔍 Диагностика проблем с сайтом

## ❌ Сайт не работает - что проверить:

### 1. Проверьте базовые вещи на хостинге:

**Через File Manager на Hostinger:**
- ✅ Файл `wp-config.php` существует в `public_html/wp-config.php`?
- ✅ База данных настроена правильно в `wp-config.php`?
- ✅ Папка темы существует: `public_html/wp-content/themes/gotry/`?
- ✅ Тема активирована в WordPress Admin → Appearance → Themes?

### 2. Проверьте ошибки PHP:

**Создайте файл `public_html/test-php.php`:**
```php
<?php
phpinfo();
?>
```

Откройте в браузере: `https://antongotry.dev/test-php.php`
- Если страница открывается - PHP работает
- Если ошибка - проблема с PHP

**Проверьте логи ошибок:**
- В File Manager найдите файлы: `error_log`, `debug.log`
- Или в панели Hostinger: Logs → Error Log

### 3. Проверьте webhook:

**Проверьте логи webhook:**
- Файл: `public_html/wp-content/webhook-theme-deploy.log`
- Откройте его и посмотрите последние записи

**Проверьте webhook на GitHub:**
1. Перейдите: https://github.com/Antongotry/gotrydev/settings/hooks
2. Нажмите на ваш webhook
3. Посмотрите "Recent Deliveries"
4. Нажмите на последний запрос
5. Проверьте "Response" - должен быть `200 OK`

### 4. Проверьте файлы темы:

**Убедитесь, что в папке темы правильные файлы:**
```
public_html/wp-content/themes/gotry/
├── style.css          ← должен быть
├── functions.php      ← должен быть
├── index.php          ← должен быть (БЕЗ WP_USE_THEMES!)
├── front-page.php     ← должен быть
├── header.php         ← должен быть
├── footer.php         ← должен быть
└── assets/           ← должна быть папка
    ├── css/
    └── js/
```

**Проверьте `index.php` темы:**
- ❌ НЕПРАВИЛЬНО: Если там `define('WP_USE_THEMES', true)` - это корневой файл!
- ✅ ПРАВИЛЬНО: Должен начинаться с `get_header();`

### 5. Проверьте права на файлы:

**Через SSH или File Manager:**
```bash
# Права на папку темы должны быть 755
chmod 755 public_html/wp-content/themes/gotry

# Права на файлы темы должны быть 644
chmod 644 public_html/wp-content/themes/gotry/*.php
```

### 6. Проверьте базу данных:

**В `wp-config.php` проверьте:**
```php
define('DB_NAME', 'имя_базы_данных');
define('DB_USER', 'пользователь');
define('DB_PASSWORD', 'пароль');
define('DB_HOST', 'localhost');
```

**Проверьте подключение к БД:**
- В панели Hostinger: Databases → MySQL Databases
- Убедитесь, что база данных существует
- Проверьте, что пользователь имеет права на базу

### 7. Проверьте .htaccess:

**Файл `public_html/.htaccess` должен содержать:**
```apache
# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
</IfModule>
# END WordPress
```

### 8. Проверьте через WordPress Admin:

1. Зайдите в: `https://antongotry.dev/wp-admin`
2. Проверьте:
   - Appearance → Themes → Тема "Gotry" должна быть активна
   - Settings → General → Site URL правильный
   - Tools → Site Health → Проверьте статус

### 9. Включите отладку WordPress:

**В `wp-config.php` добавьте (временно для диагностики):**
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

После этого проверьте файл: `wp-content/debug.log`

### 10. Проверьте через браузер:

**Откройте консоль разработчика (F12):**
- Вкладка Console - есть ли ошибки JavaScript?
- Вкладка Network - какие запросы не проходят?

## 🚨 Частые проблемы:

### Проблема: "Error establishing a database connection"
**Решение:** Проверьте настройки БД в `wp-config.php`

### Проблема: "WP_USE_THEMES already defined"
**Решение:** Удалите корневой `index.php` из папки темы

### Проблема: "404 Not Found"
**Решение:** Проверьте `.htaccess` и настройки permalink

### Проблема: Белый экран
**Решение:** Включите WP_DEBUG и проверьте `debug.log`

### Проблема: Webhook не работает
**Решение:** 
- Проверьте URL webhook на GitHub
- Проверьте секретный ключ
- Проверьте логи webhook

## 📞 Что нужно сообщить для помощи:

1. Какая именно ошибка? (текст ошибки, скриншот)
2. Что показывает браузер? (белый экран, 404, ошибка БД?)
3. Что в логах? (`error_log`, `webhook-theme-deploy.log`, `debug.log`)
4. Работает ли `wp-admin`?
5. Какие файлы есть в папке темы?
