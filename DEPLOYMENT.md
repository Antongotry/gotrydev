# Инструкция по развертыванию на Hostinger

## Текущие настройки на Hostinger

✅ **Repository:** `https://github.com/Antongotry/gotrydev`  
✅ **Branch:** `main`  
✅ **Directory:** (пусто) → будет развернуто в `public_html`

## ⚠️ Важно! Где разворачивать WordPress?

### Вариант 1: WordPress в корне сайта (public_html) - РЕКОМЕНДУЕТСЯ

Если ваш WordPress установлен прямо в `public_html`:
- **Оставьте поле "Каталог" пустым** (как сейчас настроено)
- Все файлы WordPress будут в корне: `public_html/index.php`, `public_html/wp-content/` и т.д.
- ✅ Это стандартная установка WordPress

### Вариант 2: WordPress в подпапке

Если WordPress установлен, например, в `public_html/blog/`:
- В поле "Каталог" укажите: `blog`
- Файлы будут в: `public_html/blog/index.php`, `public_html/blog/wp-content/` и т.д.

## 🔐 Файлы, которые НЕ попадут на хостинг (исключены в .gitignore)

Эти файлы останутся на хостинге и НЕ будут перезаписаны:
- `wp-config.php` - конфигурация с паролями БД
- `.htaccess` - настройки сервера
- `.private/` - приватные файлы
- `wp-content/uploads/` - загруженные медиа файлы

## 📋 Процесс развертывания

1. **После нажатия "Створити" (Создать) на Hostinger:**
   - Hostinger подключится к GitHub
   - Скачает все файлы из репозитория
   - Развернет их в указанную директорию

2. **При каждом `git push` на GitHub:**
   - Hostinger автоматически подтянет изменения (если настроен webhook)
   - Или можно настроить ручное обновление через панель Hostinger

## ⚠️ ВАЖНО! Проблема с установкой WordPress заново

### Почему WordPress пытается установиться заново?

При развертывании из GitHub Hostinger может удалять файлы в директории (включая `wp-config.php`), даже если они исключены из репозитория. WordPress не находит `wp-config.php` и запускает установщик.

### Решение: Сохраните wp-config.php ПЕРЕД развертыванием

**ШАГ 1: Сделайте бэкап wp-config.php**
```bash
# На хостинге (через SSH или File Manager):
# Скопируйте wp-config.php в безопасное место, например:
cp wp-config.php wp-config.php.backup
```

**ШАГ 2: После развертывания восстановите wp-config.php**
```bash
# На хостинге:
cp wp-config.php.backup wp-config.php
# Или создайте wp-config.php вручную через File Manager на Hostinger
```

**ШАГ 3: Альтернатива - создайте wp-config.php вручную**

1. Войдите в File Manager на Hostinger
2. Создайте файл `wp-config.php` в корне сайта (`public_html/wp-config.php`)
3. Скопируйте настройки из `wp-config-sample.php` и заполните:
   - `DB_NAME` - имя базы данных
   - `DB_USER` - пользователь БД
   - `DB_PASSWORD` - пароль БД
   - `DB_HOST` - обычно `localhost`
   - Секретные ключи (можно сгенерировать на https://api.wordpress.org/secret-key/1.1/salt/)

### Или используйте хук развертывания (post-deploy hook)

Если Hostinger поддерживает post-deploy скрипты, создайте скрипт который восстановит wp-config.php после каждого развертывания.

## ⚠️ Важные замечания

1. **Перед первым развертыванием:**
   - ⚠️ **ОБЯЗАТЕЛЬНО сделайте бэкап `wp-config.php`!**
   - Убедитесь, что папка `wp-content/uploads/` существует и имеет правильные права (755)
   - Проверьте настройки базы данных

2. **Если на хостинге уже есть WordPress:**
   - Первое развертывание может перезаписать некоторые файлы WordPress
   - **wp-config.php НЕ будет перезаписан** (исключен из репозитория), но может быть удален при очистке директории
   - Рекомендуется сделать полный бэкап сайта перед первым развертыванием

3. **После развертывания:**
   - **ПЕРВОЕ ДЕЛО:** Проверьте наличие `wp-config.php` на хостинге
   - Если его нет - восстановите из бэкапа
   - Проверьте, что сайт работает
   - Проверьте, что тема `gotry` активна
   - Проверьте, что плагины работают

## 🔄 Обновление кода

### Через Git (локально):
```bash
# Внести изменения
git add .
git commit -m "Описание изменений"
git push origin main
```

### Через Hostinger:
- Если настроен webhook → автоматическое обновление
- Если нет webhook → ручное обновление через панель Hostinger
